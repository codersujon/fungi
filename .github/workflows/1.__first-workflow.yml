name: First Workflow

on: 
  workflow_dispatch:

jobs:
    first-workflow:
        runs-on: ubuntu-latest

        steps:
            - name: First Step
              run: |
                echo "Hello, World!"
                echo "First Workflow!"

            - name: Second Step
              run: |
                echo "Yaml File"
                echo ".yml"