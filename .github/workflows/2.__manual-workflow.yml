name: Manual Workflow

on: 
    workflow_dispatch:

jobs:
    manual-workflow:
        runs-on: ubuntu-latest

        steps:
            - name: First Step
              run: |
                echo "Manual Workflow!"